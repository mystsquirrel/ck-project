@model IEnumerable<ck_project.project_status>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.project_status_name)
        </th>

        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.project_status_name)
        </td>

        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.project_status_number }) |
            @Html.ActionLink("Details", "Details", new { id=item.project_status_number }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.project_status_number })
        </td>
    </tr>
}

</table>
